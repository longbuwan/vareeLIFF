<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>ระบบประเมิน TCAS</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://static.line-scdn.net/liff/edge/2/sdk.js"></script>
  
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      background: linear-gradient(135deg, #0288d1 0%, #01579b 100%);
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Arial, sans-serif;
      min-height: 100vh;
      padding: 20px;
      overflow-x: hidden;
    }

    .container {
      max-width: 1000px;
      margin: 0 auto;
    }

    .header {
      text-align: center;
      color: white;
      margin-bottom: 40px;
      animation: fadeInDown 0.8s ease-out;
    }

    .profile-section {
      text-align: center;
      margin-bottom: 30px;
      animation: fadeIn 1s ease-out 0.2s backwards;
    }

    .profile-section img {
      width: 80px;
      height: 80px;
      border-radius: 50%;
      border: 4px solid white;
      box-shadow: 0 4px 15px rgba(0,0,0,0.2);
      margin-bottom: 12px;
      transition: transform 0.3s ease;
    }

    .profile-section img:hover {
      transform: scale(1.1);
    }

    .profile-section h2 {
      color: white;
      font-size: 1.5rem;
      font-weight: 600;
    }

    .header h1 {
      font-size: 2.2rem;
      font-weight: 700;
      margin-bottom: 12px;
      text-shadow: 0 2px 10px rgba(0,0,0,0.2);
    }

    .header p {
      font-size: 1.1rem;
      opacity: 0.95;
    }

    .features-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 25px;
      margin-top: 40px;
    }

    .feature-card {
      background: white;
      border-radius: 20px;
      padding: 35px 25px;
      text-align: center;
      cursor: pointer;
      transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
      box-shadow: 0 10px 30px rgba(0,0,0,0.15);
      position: relative;
      overflow: hidden;
      animation: fadeInUp 0.6s ease-out backwards;
    }

    .feature-card:nth-child(1) {
      animation-delay: 0.3s;
    }

    .feature-card:nth-child(2) {
      animation-delay: 0.4s;
    }

    .feature-card:nth-child(3) {
      animation-delay: 0.5s;
    }

    .feature-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
      transition: left 0.5s ease;
    }

    .feature-card:hover::before {
      left: 100%;
    }

    .feature-card:hover {
      transform: translateY(-10px);
      box-shadow: 0 20px 40px rgba(0,0,0,0.2);
    }

    .feature-card:active {
      transform: translateY(-5px);
    }

    .icon-wrapper {
      width: 80px;
      height: 80px;
      margin: 0 auto 20px;
      background: linear-gradient(135deg, #0288d1, #01579b);
      border-radius: 20px;
      display: flex;
      align-items: center;
      justify-content: center;
      box-shadow: 0 8px 20px rgba(2, 136, 209, 0.3);
      transition: all 0.3s ease;
    }

    .feature-card:hover .icon-wrapper {
      transform: rotate(5deg) scale(1.1);
      box-shadow: 0 12px 30px rgba(2, 136, 209, 0.4);
    }

    .icon {
      font-size: 2.5rem;
      filter: drop-shadow(0 2px 4px rgba(0,0,0,0.2));
    }

    .feature-card h3 {
      color: #01579b;
      font-size: 1.3rem;
      font-weight: 700;
      margin-bottom: 12px;
    }

    .feature-card p {
      color: #666;
      font-size: 0.95rem;
      line-height: 1.5;
    }

    .badge {
      display: inline-block;
      background: linear-gradient(135deg, #4caf50, #388e3c);
      color: white;
      padding: 6px 16px;
      border-radius: 20px;
      font-size: 0.8rem;
      font-weight: 600;
      margin-top: 12px;
      box-shadow: 0 4px 10px rgba(76, 175, 80, 0.3);
    }

    .badge.orange {
      background: linear-gradient(135deg, #ff9800, #f57c00);
      box-shadow: 0 4px 10px rgba(255, 152, 0, 0.3);
    }

    .badge.purple {
      background: linear-gradient(135deg, #9c27b0, #7b1fa2);
      box-shadow: 0 4px 10px rgba(156, 39, 176, 0.3);
    }

    .footer {
      text-align: center;
      color: white;
      margin-top: 50px;
      padding: 20px;
      opacity: 0.9;
      animation: fadeIn 1s ease-out 0.8s backwards;
    }

    .footer p {
      font-size: 0.9rem;
    }

    .decorative-circles {
      position: fixed;
      width: 100%;
      height: 100%;
      top: 0;
      left: 0;
      pointer-events: none;
      z-index: 0;
      overflow: hidden;
    }

    .circle {
      position: absolute;
      border-radius: 50%;
      background: rgba(255, 255, 255, 0.1);
      animation: float 20s infinite ease-in-out;
    }

    .circle:nth-child(1) {
      width: 100px;
      height: 100px;
      top: 10%;
      left: 10%;
      animation-delay: 0s;
    }

    .circle:nth-child(2) {
      width: 150px;
      height: 150px;
      top: 60%;
      right: 10%;
      animation-delay: 2s;
    }

    .circle:nth-child(3) {
      width: 80px;
      height: 80px;
      bottom: 20%;
      left: 15%;
      animation-delay: 4s;
    }

    @keyframes float {
      0%, 100% {
        transform: translateY(0) translateX(0);
      }
      33% {
        transform: translateY(-30px) translateX(20px);
      }
      66% {
        transform: translateY(20px) translateX(-20px);
      }
    }

    @keyframes fadeInDown {
      from {
        opacity: 0;
        transform: translateY(-30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
      }
      to {
        opacity: 1;
      }
    }

    @media (max-width: 768px) {
      .header h1 {
        font-size: 1.8rem;
      }

      .features-grid {
        grid-template-columns: 1fr;
        gap: 20px;
      }

      .feature-card {
        padding: 30px 20px;
      }
    }

    .container {
      position: relative;
      z-index: 1;
    }
  </style>
</head>
<body>
  <div class="decorative-circles">
    <div class="circle"></div>
    <div class="circle"></div>
    <div class="circle"></div>
  </div>

  <div class="container">
    <div class="profile-section">
      <img id="picture" src="" alt="Profile Picture">
      <h2 id="name">Loading...</h2>
    </div>

    <div class="header">
      <h1>VR The Future</h1>
      <p>ระบบประเมินความเสี่ยง</p>
    </div>

    <div class="features-grid">
      <div class="feature-card" onclick="navigateTo('pre-exam')">
        <div class="icon-wrapper">
          <div class="icon">📚</div>
        </div>
        <h3>ประเมินตนเองก่อนสอบ</h3>
        <p>ทำแบบทดสอบเพื่อประเมินความถนัด และ ความพร้อมของตัวเอง</p>
        <span class="badge">ประเมินตนเอง</span>
      </div>

      <div class="feature-card" onclick="navigateTo('tcas-round2')">
        <div class="icon-wrapper">
          <div class="icon">📙</div>
        </div>
        <h3>ประเมิน TCAS รอบ 2</h3>
        <p>ประเมินโอกาสในการเข้าศึกษาต่อในระบบ TCAS รอบที่ 2 (Quota)</p>
        <span class="badge orange">รอบ Quota</span>
      </div>

      <div class="feature-card" onclick="navigateTo('tcas-round3')">
        <div class="icon-wrapper">
          <div class="icon">📕</div>
        </div>
        <h3>ประเมิน TCAS รอบ 3</h3>
        <p>ประเมินโอกาสในการเข้าศึกษาต่อในระบบ TCAS รอบที่ 2 (Admission)</p>
        <span class="badge purple">รอบ Admission</span>
      </div>
    </div>

 
  </div>

  <script>
    window.onload = async function () {
      await liff.init({ liffId: "2007611527-z7ZNLXOk" });
      if (liff.isLoggedIn()) {
        const profile = await liff.getProfile();
        document.getElementById("name").innerText = profile.displayName;
        document.getElementById("picture").src = profile.pictureUrl;
      } else {
        liff.login();
      }
    };

    function navigateTo(page) {
      // Add a subtle click animation
      event.currentTarget.style.transform = 'scale(0.95)';
      setTimeout(() => {
        switch(page) {
          case 'pre-exam':
            window.location.href = 'https://vrthefuture.varee.ac.th/vrthefuture_pre-exam.html';
            break;
          case 'tcas-round2':
            window.location.href = 'https://vrthefuture.varee.ac.th/vrthefuture_round2.html';
            break;
          case 'tcas-round3':
            window.location.href = 'https://vrthefuture.varee.ac.th/vrthefuture_round3.html';
            break;
        }
      }, 150);
    }
  </script>
</body>
</html>
